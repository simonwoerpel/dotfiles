# http://jasonwryan.com/blog/2012/05/12/mutt/
#
# text/html; xdg-open %s; test=test -n "$DISPLAY"
# text/html; ~/.mutt/scripts/html.sh %s; test=test -n "$DISPLAY"
text/html; w3m -I %{charset} -T text/html; copiousoutput;


application/pdf; xdg-open %s; test=test -n "$DISPLAY"
application/x-bzpdf; xdg-open %s; test=test -n "$DISPLAY"
application/x-gzpdf; xdg-open %s; test=test -n "$DISPLAY"
application/x-xzpdf; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-pdf; xdg-open %s; test=test -n "$DISPLAY"
application/postscript; xdg-open %s; test=test -n "$DISPLAY"
application/x-bzpostscript; xdg-open %s; test=test -n "$DISPLAY"
application/x-gzpostscript; xdg-open %s; test=test -n "$DISPLAY"
image/x-eps; xdg-open %s; test=test -n "$DISPLAY"
image/x-bzeps; xdg-open %s; test=test -n "$DISPLAY"
image/x-gzeps; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-ps; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-eps; xdg-open %s; test=test -n "$DISPLAY"
application/x-dvi; xdg-open %s; test=test -n "$DISPLAY"
application/x-bzdvi; xdg-open %s; test=test -n "$DISPLAY"
application/x-gzdvi; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-dvi; xdg-open %s; test=test -n "$DISPLAY"
image/vnd.djvu; xdg-open %s; test=test -n "$DISPLAY"
image/vnd.djvu+multipage; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-djv; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-djvu; xdg-open %s; test=test -n "$DISPLAY"
image/tiff; xdg-open %s; test=test -n "$DISPLAY"
application/x-cbr; xdg-open %s; test=test -n "$DISPLAY"
application/x-cbz; xdg-open %s; test=test -n "$DISPLAY"
application/x-cb7; xdg-open %s; test=test -n "$DISPLAY"
application/x-cbt; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-cbr; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-cbz; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-cb7; xdg-open %s; test=test -n "$DISPLAY"
application/x-ext-cbt; xdg-open %s; test=test -n "$DISPLAY"
application/oxps; xdg-open %s; test=test -n "$DISPLAY"
application/vnd.ms-xpsdocument; xdg-open %s; test=test -n "$DISPLAY"
